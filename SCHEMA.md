# 工程规范：workbench 任务体系

> **定位**：SCHEMA = 做成什么样（workbench 任务体系目标态：目录结构/四要素边界/流转规则/产出规范）；SKILL = 怎么做（操作流程）。平级互补，SKILL 的操作实现本文定义的目标态。
> 2026-07-24 讨论定稿。任务体系跟代码走，知识产出留在知识库。

## 一、边界划分

```
项目代码仓 (<项目代码仓>/)
├── AGENTS.md                ← 项目说明书（顶层）
├── kernel/                  ← 代码
├── applications/            ← 代码
├── 0_workbench/             ← 工作台（任务管理）
│   ├── spec.md              ← 一张纸，硬约束（自带 changelog）
│   ├── design/              ← 已吸收的架构设计（合并，不堆叠）
│   ├── change/              ← 施工中
│   │   └── <change_name>/
│   │       ├── goal.md      ← 目标（改什么）
│   │       ├── scheme.md    ← 方案（怎么改）
│   │       ├── tasks.md     ← 任务（做到哪）
│   │       └── check.md     ← 验收（怎么算完）
│   └── archive/             ← 已闭环（扁平，不分成败子目录）
│       └── <change_name>/   ← 同上四要素
```

AGENTS.md 在代码仓顶层——无论 workbench 是否存在，AI 都能独立工作。workbench 是任务管理的附加层，不绑定 AI 入口。

```
知识库 (<知识库>/)
└── projects/<项目>/
    └── references/          ← 分析报告、基线、实验数据
        └── YYYYMMDD-标题.md ← 扁平，不分子目录
```

## 二、change 四要素

四要素同目录 `change/<name>/`，归档随目录迁 `archive/<name>/`，**文件名不变**，统一 goal/scheme/tasks/check。

| 文件 | 一句话 | 内容骨架（按实践） |
|------|--------|----------|
| goal.md | 目标/预期 | 为什么做（现状/痛点）→ 做成什么样（成功标准：量化表 维度/基线/目标，**标准在此定**）→ 副作用约束（边界原则，不碰什么）|
| scheme.md | 怎么改 | 事实约束表（API/内核/硬件可用性）→ 现状路径 → 目标路径 → 核心设计 → 资源预算 → 风险表 → 改动范围（文件级清单）|
| tasks.md | 做到哪 | 分阶段任务（编号如 P1.1a，`[ ]/[x]/[!]`，含测试任务）→ 每阶段记 commit 短 hash → **测试数据**（基线/对比表，单一权威源）→ **验收结果**（对照 goal 标准，✅/❌+结论）→ 可选优化 |
| check.md | 怎么验 | 验收方法（可执行命令，按 编译/功能/性能/稳定性/内存 分类组织，**专一存放，不含结论、不重定标准**）|

> **抽象/具象分层**：goal/check/scheme 是规划态的抽象（目标、验收方法、方案）——想清楚；tasks 是执行态的具象——commit、测试数据、验收结果皆落 tasks，做出来并落定。前三者定「该是什么样、怎么验、怎么改」，tasks 落地为具体代码基线、实测数字、✅/❌ 结论。

**确认顺序：goal → check → scheme → tasks**（目标→验收方法→方案→任务）。先定 goal 标准，再定 check 方法，确保目标可衡量、方案不漂；最后设计 scheme、拆 tasks。验收驱动——先把「怎么算完」想清楚，再想「怎么做」。

> 区分：确认顺序 ≠ 执行/回填顺序。执行时跑 tasks（用 check 方法测），实测回填 tasks「验收结果」对照 goal 标准。

### 四要素边界约定（防重叠/冲突）

- **标准由 goal 定，方法由 check 给，结果落 tasks**：goal 成功标准是唯一阈值来源；check 只给验收方法（怎么测），不含结论、不重定标准；验收结果（实测 vs 标准的 ✅/❌）落 tasks 末尾。check 不得出现 goal 之外的门槛（如 goal ≥94 则 check 不得写 ≥90）。若需底线值，goal 表内显式分「目标/底线」两列。
- **方法只放 check**：验证/验收的可执行命令专一放 check「验收方法」。scheme 不写验证方法，只写方案本身的「观测点」（本方案需特别看什么，如 pool recycle/fallback 计数）。
- **边界 vs 清单**：goal「副作用约束」写边界原则（不碰 rxbuff/协议栈/网络栈），scheme「改动范围」写精确文件清单。同一信息不两处复述。
- **基线权威源在 tasks**：基线数字以 tasks「测试数据」为准，goal 成功标准表、check 验收的基线列引用此处，不另引别处基线。
- **验收结果落 tasks**：最终验收结论在 check，但「对照结果」（实测值 vs 成功标准表）写 tasks 末尾「验收结果」小节，与测试数据同处，便于一眼看完。

### 拒绝态（证伪/废弃）

归档不分成败。证伪的 change 四要素照留，额外标注：
- goal.md 顶部 `> ⚠️ 证伪，状态: 拒绝` + 一句话否决理由
- scheme.md 加 §证伪记录（否决链 + 工作区处理：代码是否回退）
- tasks.md 验收结果记 `❌ 未通过（证伪）` + 实测对照（check 仍只存方法）

### retroactive（先干后补）

change 已施工再补四要素时，goal.md 顶部标 `> retroactive：已施工，归档时补写`。不鼓励作为常规，但容忍。

### tasks.md commit + 测试数据 + 验收结果约定

每个阶段任务完成时记本地 git commit 短 hash，作该阶段代码基线供回退对比；测试任务是 tasks 的一部分（如「P1.5 性能测试」阶段，方法见 check）；测试数据（iperf/重传/内存等基线与对比）落 tasks「测试数据」小节作单一权威源；最终对照成功标准的「验收结果」落 tasks 末尾，与测试数据同处。

```markdown
## P1 基线测量
- [x] 编译烧录 → `a1b2c3d`
- [x] iperf3 RX 测试 → `a1b2c3d`

## 测试数据
### 基线（2026-07-28，改动前）
| 指标 | 本次 | 基线 | 目标 |
| 吞吐 | 78 Mbps | 115 Mbps | ≥94 |

## 验收结果
| 维度 | 目标(goal) | 实测 | 判定 |
| 吞吐 | ≥94 | 78 | ❌ |
| 稳定性 | 0 crash | 0 | ✅ |
最终：❌ 未通过（证伪，详见 check）
```

## 三、流转规则

```
change/ 施工中
    ↓ check.md 验收
    ├── 通过 → 归档 archive/（按序执行，每步不可跳过）
    │         1. 四要素随目录迁 archive/<name>/（文件名不变，留快照）；
    │            scheme 要点同时吸收进 design/（合并到现有文档，不堆文件）
    │         2. 复审 spec.md 全文 → 判断本次 change 是否新增/更新约束、
    │            已知天花板、配置项；有则更新并在「变更记录」记一行
    │            （归档必检，非"按需"——漏更新 spec = 归档未完成）
    │         3. 产出物    → 写入知识库 references/
    │
    └── 不通过 → 回到 scheme.md / tasks.md 继续迭代，不归档
```

产出物包括：分析报告、性能基线、实验数据、调试笔记。

> 归档检查清单：① scheme 已吸收进 design ② spec 已复审并按需更新 ③ 产出物已落知识库。三项全绿才算归档完成。

## 四、references 规范

**命名**: `YYYYMMDD-标题.md`

**文件头部元数据**:
```markdown
---
标题: RX基线测量
日期: 2026-07-24
来源: wifi_driver_refactor
状态: 接受
吸收至: design/wifi_driver_design.md §3, spec.md §性能
摘要: iperf3 TCP RX 基准测试，52→82Mbps 优化路径验证
---
```

**状态**: 仅两态

| 状态 | 含义 |
|------|------|
| 接受 | 结论已被吸收，可引用 |
| 拒绝 | 不采纳，此路不通，立警示 |

**吸收至**: 标注被吸收到了哪个 design 文件或 spec 的哪条约束。拒绝状态可省略。

## 五、spec.md 维护

- 跟随 workbench，单一文件，不可拆为文件夹
- 自带 changelog（不依赖 git log），格式如下：

```markdown
## 变更记录
2026-07-24 | wifi_driver_refactor | RX 天花板从 52Mbps → 82Mbps
2026-07-20 | partition_fix       | 约束分区大小，禁止手动 resize
```

- design 吸收方案后，更新 spec 约束
- **归档必检**：每次 change 归档时强制复审 spec 全文（流转规则第 2 步），判断约束/天花板/配置是否要新增或更新，并在 changelog 记一行。不依赖"按需"想起——漏更新 spec 即归档未完成

## 六、design 吸收原则

合并而非堆叠——更新现有设计文档对应章节，末尾标注来源 change（如 `> 来源: wifi_driver_refactor`）。design/ 始终是可读的当前架构，不是方案坟场。

## 七、实施清单

- [ ] 在项目代码仓创建 `0_workbench/` 结构
- [ ] 从知识库迁移现有 archive/change/design/spec 到 0_workbench
- [ ] 清理知识库 projects/，仅保留 references/
- [ ] 确认各项目代码仓顶层已有 AGENTS.md
