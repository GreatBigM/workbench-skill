# <change_name> — 验收

> 怎么算完：可执行命令，按类组织。**只放方法，不含结论、不重定标准**——标准在 goal，结论落 tasks。

## 编译

```bash
<编译命令 1>
<编译命令 2>
```

## 功能

```bash
<功能验证命令/操作步骤>
<预期结果描述>
```

## 性能

```bash
<性能测试命令（如 iperf3 -c <host> -t 30 -P 5）>
```

## 稳定性

```bash
<压力/循环测试命令（如 N 次重启循环）>
<crash 判定方法>
```

## 内存

```bash
<内存测量命令（free / cat /proc/meminfo / 泄漏检查）>
```
